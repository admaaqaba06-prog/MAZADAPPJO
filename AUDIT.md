# AUDIT.md — المرحلة صفر: استكشاف قبل أي تعديل

> تدقيق **قراءة فقط**. لم يُعدّل أي سطر كود، ولم يُنشر أي شيء.
> نُفِّذ بـ٦ وكلاء متوازيين + ناقد نهائي، ٣٢٥ عملية قراءة على الكود.
> كل سطر أدناه مُتحقَّق منه بقراءة الكود، ومعه `file:line`.

---

## ٠. الخلاصة التنفيذية — اقرأ هذا أولاً

| # | النتيجة | الخطورة |
|---|---|---|
| 1 | **المرحلة 1.2 (إظهار السعر الأدنى) غير قابلة للتنفيذ كما هي** — الرقم محفوظ في مجموعة يقرأها الأدمن فقط | 🔴 حاجب |
| 2 | **المرحلة 1.4 (نسبة الرفض) خطِرة كما هي** — الدالة المطلوب تعديلها تكتب أيضاً حقلاً يقرأه الجميع | 🔴 حاجب |
| 3 | **نصوص المرحلة 4.2 تُناقض سياسة المزادات المنشورة حالياً** — ستصبح غير صحيحة | 🔴 حاجب |
| 4 | **ثغرة ترسية ثانية لم تُذكر**: `repairEndedAuctionOrder` تُنشئ الطلب دون قراءة السعر الأدنى إطلاقاً | 🔴 حاجب |
| 5 | **أسماء ٥ حقول في طلبك غير موجودة** في الكود بهذه الأسماء | 🟠 عالٍ |
| 6 | **شارة «حساب موثّق» بلا معنى** — كل حساب جديد يُوسم `isVerified: true` تلقائياً | 🟠 عالٍ |
| 7 | **بوابة «لا يُنشر منتج بلا صور» ميتة** — لا يمكن أن تُرجع false أبداً | 🟠 عالٍ |
| 8 | **حاويتا تخزين (buckets)، واحدة فقط لها قواعد أمان** | 🟠 عالٍ |
| 9 | سكربت الـbackfill المطلوب في 2.3 **موجود مسبقاً** | 🟢 توفير عمل |

---

## ١. أسماء الحقول الحقيقية مقابل ما ورد في الطلب

| ما ورد في طلبك | الواقع في الكود | المرجع |
|---|---|---|
| `reservePrice` على المزاد | ❌ غير موجود على المزاد. في مجموعة **`auctionSecrets/{auctionId}`** | `firestore.rules:278-284` |
| `currentBid` | ❌ الاسم الحقيقي **`currentPrice`** (`currentBid` مجرد نص واجهة) | `src/types.ts:98` |
| `winnerId` | ❌ الاسم الحقيقي **`currentBidderId`** | `src/types.ts:100` |
| `coverImage` | ❌ غير موجود على المزاد (موجود على `SellerProfile` فقط). الحقل الحقيقي **`thumbnailUrl`** | `src/types.ts:67` |
| `showReservePrice` | ❌ غير موجود إطلاقاً — صفر نتائج في كل المستودع | — |
| `status` | ✅ موجود لكن بـ٧ قيم: `upcoming \| live \| processing \| rejected \| completed \| ended \| reserve_not_met` | `src/types.ts:123` |
| `sold` / `no_bids` / `pending_approval` / `rejected_by_seller` | ❌ **لا وجود لأي منها**. الواقع: البيع = `completed`، بلا مزايدات = `ended` | `functions/index.js:479,596` |
| `endTime` | ⚠️ **قابل أن يكون `null`** لمزادات `startMode:'first_bid'` — أي حساب رياضي عليه بدون حماية يعطي `NaN` | `src/types.ts:115` |

**تنبيه:** هناك **شكلان متعارضان** للمزاد في الكود: `AuctionItem` (`src/types.ts:92`) و`FirebaseAuction` (`src/services/auctionService.ts:4`). الثاني فيه `winnerId` و`endsAt`، الأول لا.

---

## ٢. المرحلة 1 — الترسية

### 2.1 أين تُغلق المزادات فعلياً

- **مسار واحد رئيسي:** `exports.scheduledAuctionCloser` — `functions/index.js:694`، مجدولة **كل دقيقة**، تنادي `settleAuctionTxn` في السطر 765.
- **الدالة نفسها:** `settleAuctionTxn` — `functions/index.js:360`.
- **القرار الصافي:** `resolveSettlement` — `functions/settlement.js:19`.
- **لا يوجد** أي منطق ترسية في الواجهة. `LiveStreamView.tsx:106-110` فيه تعليق صريح بأن الواجهة يجب ألا تكتب `status:'completed'`، وهي فعلاً لا تكتبه.

### 2.2 ما يعمل بشكل صحيح اليوم ✅

- المقارنة **على الخادم فقط**. المبلغ يُقرأ من `auctionSecrets` (`index.js:398-405`) ويفشل مغلقاً عند خطأ قراءة (`throw secErr` سطر 404).
- الترسية داخل **transaction** (`index.js:414`)، مع إعادة قراءة داخلية (419-420) وحماية من الترسية المزدوجة (سطر 422).
- إنشاء الطلب محمي بـ`if (!orderSnap.exists)` (سطر 491).
- **إشعار الفوز مربوط بالنتيجة لا بالوقت** — `notifyData` تُملأ فقط داخل `if (decision.outcome === 'sold')` (540-551) والإرسال بعد الـcommit (622-684). هذا يطابق طلبك أصلاً.
- حماية `reserveIntended` المضافة حديثاً موجودة في `settlement.js:47-49` مع ٦ اختبارات (`settlement.test.js:149-191`).

### 2.3 ثغرات حقيقية اكتشفها التدقيق 🔴

| # | الثغرة | المرجع |
|---|---|---|
| A | **مسار ترسية ثانٍ يتجاهل السعر الأدنى تماماً**: `repairEndedAuctionOrder` تُنشئ الطلب مباشرة، **لا تقرأ `auctionSecrets` ولا تنادي `resolveSettlement`**، وليست داخل transaction | `functions/index.js:3006` |
| B | `reserveIntended` تُحسب من **اللقطة قبل الـtransaction** لا من `freshData` — نافذة تلاعب/تقادم | `functions/index.js:462` |
| C | **مبلغ السعر الأدنى يُقرأ خارج الـtransaction** (398-405 قبل 414) | `functions/index.js:398` |
| D | `reservePrice = 0` يمرّ كـ`0` فيُعتبر «بلا حد أدنى» — الحماية تفحص `null/undefined` فقط | `settlement.js:47` |
| E | حماية الترسية المزدوجة تفحص `status` فقط، **لا تفحص `settledAt`** | `functions/index.js:422` |
| F | `reserveMet` **غير محمي بالقواعد** — `moneyTimingKeys()` لا يشمله، فمتصفح أدمن يستطيع حذفه | `firestore.rules:129-135` |
| G | لا فحص وجود للمستند: `freshData.status` على `undefined` يرمي **داخل** الـtransaction | `functions/index.js:420-422` |

### 2.4 المرحلة 1.3 (`pending_approval`) — تعارضات يجب حلها قبل التنفيذ

1. **يوجد نظام قبول/رفض جاهز بالفعل** ولم يُذكر في طلبك: `acceptBelowReserve` (`index.js:3158`) / `confirmBelowReserve` (3308) / `declineBelowReserve` (3415)، مع `belowReserveOffer` (`types.ts:171-181`) وحالاته `pending_seller | pending_buyer | confirmed | declined`، ونافذة **`BELOW_RESERVE_WINDOW_HOURS = 24`** (`settlement.js:191`). **التوصية: نوسّعه لا نبني بديلاً.**
2. **`pending_approval` سيُخفي المزاد عن المشتري**: `useMyAuctionLots.ts:59-60` يرشّح `status in ['live','completed']` فقط.
3. **تعارض تسمية خطير**: `listingApproval.js:16-19` يعرّف `approvalStatus === 'approved'` بمعنى مختلف تماماً — وهو **يمنح حساب بائع**.
4. **قبول البائع من المتصفح مستحيل**: `firestore.rules:255-257` تمنعه ⇒ يجب أن يكون **callable**.
5. `APPROVAL_WINDOW_HOURS` غير موجود، ولا يوجد أي sweep للمهل.
6. المفتاح `pending_approval` موجود كترجمة **ميتة** في `SellerCenterView.tsx:153,272` — بلا أي استخدام.

### 2.5 المرحلة 1.4 (نسبة الرفض) — 🔴 خطِرة كما هي

`reserveMet` **ليست دالة الترسية فقط**: `functions/index.js:1687-1691` تناديها داخل `onBidCreated` ثم تكتب **الحقل العام المقروء للجميع** `reserveMet` على مستند المزاد. أي إضافة «نسبة تسامح» داخلها تعني أن الشارة العامة ستقول «تم تجاوز الحد» بينما الحد لم يُتجاوز فعلياً.

**السؤال الذي أحتاج جوابه منك (مثال رقمي):** سعر أدنى **1000**، تسامح **10%**:
- أعلى مزايدة **950** ⟶ أقل من 1000 لكن ≥ 900 ⟶ هل **يُعرض على البائع للقبول**؟
- أعلى مزايدة **880** ⟶ أقل من 900 ⟶ **يُرفض آلياً ولا يظهر كخيار**؟

إن كان هذا فهمك، فالتنفيذ الصحيح هو **بند منفصل في قرار الترسية**، وليس تعديل `reserveMet` — لكي لا تتأثر الشارة العامة.

---

## ٣. المرحلة 2 — الوسائط

### 3.1 تشخيص فشل الرفع (الأسباب الفعلية)

| # | السبب | المرجع |
|---|---|---|
| 1 | **٣ فقط من ١٧ مسار رفع تستخدم resumable**. كل صور المعرض تستخدم `uploadBytes` أحادي الطلقة ⇒ أي انقطاع شبكة = فشل كامل | `ListingWizardView.tsx:160`, `AuctionDropBuilderView.tsx:335`, `SellView.tsx:155` |
| 2 | **صفر معالجة لـHEIC/HEIF** — الكلمات لا تظهر إطلاقاً في `src/` | — |
| 3 | **٥ مسارات رفع بلا أي فحص حجم** أمام قاعدة 10MB ⇒ الملف يُرفع كاملاً ثم يفشل برسالة غامضة | `storage.rules` |
| 4 | `catch` صامت في ٣ مواضع لصور المعرض ⇒ **فشل بلا رسالة** | — |
| 5 | **حاويتان (buckets)، واحدة فقط لها قواعد** | `services/firebase.ts:15,33-36` |
| 6 | **لا يوجد أي Cloud Function للمعالجة بعد الرفع** (لا resize ولا thumbnail) ⇒ لا timeout هنا، لكن الغياب نفسه نتيجة | — |
| 7 | **لا توجد مجموعة `uploadErrors`** ⇒ لا سجل دائم لأي فشل | — |
| 8 | ترتيب الكتابة **سليم** (الرفع يُنتظر قبل `setDoc`) — لكن رابط `blob:` قد يصل Firestore بمسار آخر | `AppContext.tsx:3932,3960,4089` |
| 9 | `.MOV` مقبول بالرفع لكن **ليس مضموناً التشغيل** في المتصفح | `mediaPickerState.ts:192-199` |
| 10 | الامتدادات الكبيرة `.JPG` **ليست مشكلة** — مسار الصور يفحص `f.type` لا الاسم | `mediaPickerState.ts:28-30` |

### 3.2 قواعد التخزين الحالية (تحتاج موافقتك قبل أي تعديل)

`auction-videos/{fileName}` (`storage.rules:57`) و`auction-thumbnails/{fileName}` (`:68`):
- الكتابة مشروطة بـ**`isSignedIn()` فقط** — لا `isOwner`، ولا uid في المسار
- القراءة **`allow read: if true`** (عامة للجميع)

### 3.3 صورة الغلاف — 🟠 نتيجة مهمة

**بوابة «لا يُنشر منتج بلا وسائط» ميتة ولا يمكن أن تعمل**: `auctionDocMap.ts:45-49` تستبدل السلسلة الفارغة بصورة بديلة، فـ`docHasMedia` تُرجع `true` **دائماً**.

**وسكربت الـbackfill الذي طلبته موجود مسبقاً:** `scripts/admin/backfill-stock-covers.cjs` (136 سطراً)، تقريري افتراضياً، يُطبّق فقط بـ`--apply`.

---

## ٤. المرحلة 3 — تقييد المزايدات

**لا يوجد أي تقييد حقيقي على الخادم.** الموجود فقط: فاصل **1.5 ثانية** بين مزايدتين (`functions/index.js`, `lastBidAt`).

| المطلوب | الواقع |
|---|---|
| حد ساعة/يوم على الخادم | ❌ غير موجود |
| حد 10 مزايدات/دقيقة | ⚠️ موجود **في المتصفح فقط وفي الذاكرة** — يُخدع بإعادة تحميل الصفحة |
| حد لكل مزاد | ❌ غير موجود |
| `rateLimits/{userId}` | ❌ غير موجود (القواعد تعرّف ٢٢ مجموعة، ليست فيها) |
| `bidBannedUntil` | ❌ غير موجود. **الحقل الحقيقي `blockedUntil`** (`types.ts:25`) |
| `abuseLog` | ❌ غير موجود |
| شاشة أدمن للمحظورين | ❌ لا توجد. والقائمة الحالية **محدودة بـ200** مرتبة بـ`lastSeen` ⇒ عاجزة بنيوياً عن عرض كل المحظورين |

**تحذير من الناقد:** رسالة خطأ عربية جديدة **ستُغرق لوحة صحة النظام** — `bidErrors.ts:27-30` يطابق نصوصاً **إنجليزية** فقط، فأي رفض عربي سيُحسب خطأً غير متوقع.

---

## ٥. المرحلة 4 — تجربة المستخدم

### 5.1 خيار VIP الدفع عند الاستلام

**لا يوجد حقل في قاعدة البيانات إطلاقاً** — لا `payOnDelivery` ولا `cashOnDelivery` ولا `paymentMethod`. فطلبك «أبقِ الحقل واحذفه من الواجهة» **لا ينطبق: ليس هناك حقل**. الموجود **نصوص فقط**:

- `src/components/SubscriptionView.tsx:104` و`:110` ⟵ **واجهة عميل حقيقية**
- `src/utils/dropCaption.ts:29` ⟵ داخل `TERMS` (أدوات أدمن)
- مؤكَّد في المخرَج المبني: `dist/assets/SubscriptionView-*.js`

### 5.2 نصوص شروط المزايدة — 🔴 تُناقض السياسة الحالية

| نص مطلوب | التعارض |
|---|---|
| «كون المستخدم آخر مزايد لا يعني إتمام البيع» | `settlement.js:50-51` تكتب `completed` **فوراً** ⟶ النص **غير صحيح** اليوم |
| رابط لسياسة المزادات | `src/content/auctionRules.ts:26` تقول: «صاحب أعلى مزايدة صحيحة عند إغلاق المزاد هو الفائز» ⟶ **تناقض مباشر مع الرابط نفسه** |
| «الأسعار المعروضة أسعار بداية فقط» | `bidLabels.ts:15-16` يعرض «السعر الافتتاحي» **فقط** عندما `totalBids === 0`، وبعدها «المزايدة الحالية» ⟶ النص **غير دقيق** بعد أول مزايدة |

**⇒ نصوص 4.2 لا يجوز نشرها قبل تنفيذ 1.3.** وإلا نكتب وعداً يخالف الكود والسياسة.

`AuctionTermsNotice` غير موجود. و`BidConfirm.tsx` **ليس نافذة (modal)** — تعليقه في `:62` يقول ذلك — وليس فيه checkbox، ويُلغي نفسه تلقائياً.

---

## ٦. المرحلة 5 — النماذج

| المطلوب | الواقع |
|---|---|
| الفيديو اختياري | ❌ **إلزامي بصلابة**: `ListingWizardView.tsx:116-119` يمنع الإرسال بـ`alert('الرجاء رفع فيديو أولاً.')` |
| تحقق خادم للفيديو | ℹ️ **لا يوجد تحقق خادم للإدراج إطلاقاً** — المتصفح يكتب المستند مباشرة (`AppContext.tsx:4087`) |
| التوثيق اختياري | ✅ **اختياري فعلاً** للتصفح والمزايدة والإدراج (`guestGate.ts:71-78`) |
| زر «تخطّى الآن» | ❌ غير موجود — النافذة فيها زر إغلاق X فقط |
| شارة «موثّق» ذات معنى | 🔴 **بلا معنى**: `functions/index.js:2894` يضع `isVerified: true` **لكل حساب** عند التسجيل |
| مفتاح إعدادات لاشتراط التوثيق | ❌ غير موجود |
| قاعدة تمنع غير الموثّقين | ✅ **لا توجد** — لا شيء سيفشل بصمت |

---

## ٧. أسئلة أحتاج جوابك عليها قبل البدء

1. **المرحلة 1.2:** السعر الأدنى سرّي بقاعدة أمان. أي مسار تريد؟ **(أ)** شارة فقط بلا رقم *(الأسهل والأسلم، وممكنة اليوم)* · **(ب)** نقل الرقم لمستند عام *(يكشفه للجميع)* · **(ج)** دالة خادم تُرجع الرقم لمن يحق له
2. **المرحلة 1.4:** هل فهمي للمثال الرقمي (1000 / 10% / 950 مقبول / 880 مرفوض) صحيح؟
3. **المرحلة 1.3:** أوسّع نظام `belowReserveOffer` الموجود، أم أبني `pending_approval` منفصلاً؟ *(توصيتي: التوسيع)*
4. **المرحلة 4.2:** أؤجّل النصوص حتى بعد 1.3؟ *(توصيتي: نعم — وإلا ننشر نصاً غير صحيح)*
5. **HEIC:** التحويل يحتاج مكتبة جديدة. أطرحها عليك عند الوصول للمرحلة 2.
