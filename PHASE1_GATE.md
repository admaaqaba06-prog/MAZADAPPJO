# PHASE 1 — البوابة العدائية النهائية

> ٦ مراجعين معادين، كلٌّ على فئة إخفاق. **٦ من ٦ أعادوا FAIL.**
> **٧٥ خطراً · ٢٦ حاجباً · ٢٨ عالياً · ٦١ ادّعاءً ثبتت سلامته.**
> لم يُعدَّل أي كود إنتاجي. لم تُرحَّل بيانات. لم يُنشر شيء.

---

# ١. الحكم: ❌ **FAIL**

**الخطة كما هي غير آمنة للنشر.** ليس بسبب تفاصيل تنفيذ — بل لأن **المسار «أ» لا يمكن أن يحدث أصلاً**.

---

# ٢. الحواجب — مجمّعة بالسبب الجذري

## 🔴 ج-١ — لا يوجد مميِّز بين المسارين، فالمسار «أ» لا يقع

`acceptBelowReserve` (`index.js:3461-3550`) **حمولته ثابتة**: `status:'pending_buyer_confirmation'`, `belowReserve:true`, بلا `paymentDeadlineAt`. لا يقرأ شيئاً يميّز «بلغ الحد» عن «تحت الحد».

**النتيجة:** كل بيعة بلغت حدّها الأدنى تُوجَّه إلى مسار تأكيد المشتري. **المسار «أ» لا يوجد.** وأسوأ: الفائز بمزاد بلغ حده يُعطى **زر انسحاب** من بيعة تصفها قواعدك بأنها مُلزِمة.

و`belowReserveOffer` **لا يحمل أي حقل** يقول أي مسار هو (`index.js:644-650`, `types.ts:181-201`).

## 🔴 ج-٢ — `pending_seller` بلا مخرج

`index.js:550` هو **الموضع الوحيد** في كل دوال السحابة الذي يكتب `auctions.status='completed'` — **تحققتُ بنفسي** (`:4620` طلب، `:5740` سحب). الخطة تنقله خارج فرع الإغلاق ⟹ **لا شيء يستطيع كتابته بعد ذلك**.

و`rejectBelowReserve` (`:3657`) و`expireLapsedOffers` (`:1290`) يكتبان **حقل العرض فقط، لا `auction.status`**. فكل قطعة يرفضها البائع أو يتجاهلها **تبقى `pending_seller` للأبد** — خارج كل سلة بائع، ولا تُحتسب غير مباعة، ولا تُعاد إدراجاً.

## 🔴 ج-٣ — الإشعارات تُطلَق عند الإغلاق، والخطة لم تمسّها

`index.js:706-757` يُطلق `auction_won` + `payment_due` + دفعة FCM **عند الإغلاق**. الخطة لم تذكر هذه الكتلة إطلاقاً.

⟹ **كل فائز يُطالَب بالدفع — واتساب وإيميل ودفعة وجرس — لشراء لم يوجد بعد وقد لا يوجد أبداً.** و«٢٤ ساعة» في النص تبدأ قبل المهلة الحقيقية بـ٢٤ ساعة.

**وأخطر منه:** فرع البيع **لا يُشعِر البائع إطلاقاً**. `below_reserve_offer` موجود في فرع `reserve_not_met` فقط (`:659-673`). ⟹ **مهلة ٢٤ ساعة تُفرض على بائع لم يُخبَر بوجودها.**

## 🔴 ج-٤ — `expireLapsedOffers` (كودي أنا، منشور اليوم)

ثلاثة عيوب متراكبة:

1. **سباق يُفقد بلا أمان.** `index.js:1291` كتابة عارية `docSnap.ref.update()` — بلا معاملة، بلا إعادة قراءة، بلا شرط. المراجع أثبت الاتجاه: **إن كان القبول قد أُثبِت أولاً، تكتسح الكنسة كتابته** وتُبقي طلباً بـ`waiting_payment` ومهلة حيّة بينما العرض يقول «منتهٍ».
2. **الساعتان تتخالفان.** `offerDeadlineMs` تشتقّ مهلة احتياطية؛ `isBelowReserveOfferExpired` تُرجع `false` لتاريخ تالف. **اختباري نفسه يوثّق الخلاف** (`offerExpiry.test.js:57`).
3. **`offeredAt` لا يكتبه أحد.** المراجع فتّش المستودع كله: **صفر كاتب**. سلسلة الاحتياط تشير إلى حقل غير موجود.

**والأسوأ:** بعد ٢٤ ساعة تقول لفائز بمزاد **بلغ حدّه**: «لم يقبل البائع مزايدتك» (`messageCopy.js:112-113`). **كذبة قابلة للتصوير.**

## 🔴 ج-٥ — `authorizeOrderRepair` (كودي أنا، #292)

- **يرفض كل قطعة رابحة ٢٤ ساعة** بفرع `offer_pending` ⟹ العلاج اليدوي الوحيد معطّل **بالضبط حين يُحتاج**.
- **وثغرة عكسية:** الحارس يعرف `pending` فقط، لا `declined` ⟹ **يستطيع أدمن أن يبيع قطعة رفضها البائع صراحةً**، فيُكتب طلب `waiting_payment` ويُطلق `auction_won`+`payment_due` على **كل القنوات**.

## 🔴 ج-٦ — مشتريان لسلعة واحدة

`pending_seller` ليست في `SETTLED_STATUSES` (`approvalGuard.ts:30`) ⟹ «الموافقة والنشر» تُعيد فتح قطعة عليها عرض حيّ. ثم يبيعها `acceptBelowReserve` للمزايد القديم بالسعر القديم **بينما مزاد جديد يستقبل مزايدات**. وإعادة التسوية تصطدم بـ`if (!orderSnap.exists)` فلا تُنشئ طلباً للفائز الجديد.

## 🔴 ج-٧ — واجهة المشتري تقول «فزت، ادفع خلال ٢٤ ساعة» طوال نافذة الرفض

`bidMath.ts:42` + `LiveStreamView.tsx:119,170,193-218` + `translations.ts:273-277`.

## 🔴 ج-٨ — واجهة البائع تكذب ثلاث مرات

`SellerCenterView.tsx:1534-1547`: تقول لبائع **بلغ حدّه** إن المزايدة دونه · وعلى قطعة **بلا حد أصلاً تخترع حداً** (وهو ما وُجد `reserveStatus.ts:24-28` لمنعه) · وتَعِد بخطوة تأكيد مشترٍ **لا وجود لها في المسار أ**.

## 🔴 ج-٩ — رفع `RULES_VERSION` لا يُعيد سؤال أحد

**تحققتُ بنفسي:** `acceptedAuctionRulesVersion` يظهر **مرة واحدة في كل `src/`** — كتابةً (`AppContext.tsx:3337`). **لا أحد يقرأه.**

⟹ سؤالك في §14 جوابه: **الآلية غير موجودة.** الرفع إلى `2` **إجراء شكلي** لا يُعيد مطالبة أي عضو قائم. وقواعدك المنشورة تقول «صاحب أعلى مزايدة صحيحة عند إغلاق المزاد هو الفائز» — وستصير كذباً منشوراً.

---

# ٣. ضمانات ما قبل النشر — إلزامية

| # | الضمانة | يعالج |
|---|---|---|
| ١ | **مميِّز على العرض**: `path: 'reserve_met' \| 'below_reserve'` يُختم عند الإغلاق، و`acceptBelowReserve` يتفرّع عليه | ج-١ |
| ٢ | **كل مخارج `pending_seller` تكتب `auction.status`** — القبول ⟶ `completed`، الرفض والانقضاء ⟶ حالة نهائية. وتُضاف إلى حارس `:446` وإلى `SETTLED_STATUSES` | ج-٢، ج-٦ |
| ٣ | **نقل كتلة الإشعارات** من الإغلاق إلى القبول، **وإضافة إشعار للبائع** بأن أمامه قراراً | ج-٣ |
| ٤ | **`expireLapsedOffers` داخل معاملة** مع إعادة فحص الحالة · **توحيد ساعتَي الانتهاء** · **حذف `offeredAt`** من سلسلة الاحتياط | ج-٤ |
| ٥ | **`authorizeOrderRepair`**: يفرّق `pending` عن `declined`، ويسمح بالإصلاح على المسار «أ» | ج-٥ |
| ٦ | **سكربت الإصلاح** مكتوباً ومُختبَراً و**مُجرَّباً جافاً على الإنتاج قبل نشر الدوال** | التراجع |
| ٧ | **آلية قراءة `acceptedAuctionRulesVersion`** — وإلا فالرفع بلا معنى | ج-٩ |
| ٨ | **الإحصاء مُشغَّلاً** (`phase1-census.cjs`) وQ5 صفر | الفرضية |

---

# ٤. آلة الحالات النهائية

```
live ──► settleAuctionTxn
  │
  ├─ لا مزايدات ──────────────► 'ended'
  ├─ تحت أرضية التسامح ───────► 'reserve_not_met'  (بلا عرض)
  └─ مزايدة صالحة ────────────► 'pending_seller'
         belowReserveOffer{ status:'pending_seller', path:'reserve_met'|'below_reserve' } 🆕
         + إشعار للبائع 🆕   + «أنت أعلى مزايد» للمشتري   ❌ لا auction_won ❌ لا payment_due
              │
   ┌──────────┼──────────┬─────────────┐
 قبول(أ)    قبول(ب)     رفض          انقضاء
   │          │           │             │
offer=      offer=      offer=       offer=
'confirmed' 'pending_   'declined'   'expired'
auction=     buyer'     auction=     auction=
'completed' auction يبقى 'ended' 🆕   'ended' 🆕
طلب waiting  'pending_   لا طلب       لا طلب
_payment     seller'     لا دفع       لا دفع
مهلة=القبول  طلب pending_             إشعار للطرفين 🆕
+auction_won _buyer_conf.
+payment_due  بلا مهلة
   │          │
   │     ┌────┴────┐
   │  تأكيد     رفض المشتري
   │     │          │
   │ offer='confirmed'  لا بيع
   │ auction='completed'
   │ waiting_payment
   │ مهلة=التأكيد
   └─────┴──► مدفوع → تجهيز → شحن → تسليم → مكتمل
```

**المرجعية:** `belowReserveOffer.status` يحكم **مَن يجوز له التصرف**. `auction.status` يحكم **العرض والفرز**. الطلب يحكم **هل هذا بيع**.

---

# ٥. ترتيب النشر

المثال الذي اقترحتَه **خاطئ في موضع واحد**: أدوات الإصلاح أولاً صحيح، لكن **الواجهة يجب أن تسبق التسوية** لا أن تليها.

```
١  سكربت الإصلاح          ← مكتوب ومُختبَر ومُجرَّب جافاً. لا يُنشر شيء قبله
٢  إصلاحات مستقلة          ← expireLapsedOffers معاملاتية · authorizeOrderRepair
                             (كلاهما إصلاح لعيب حيّ اليوم، قيمته مستقلة)
٣  الواجهة أولاً 🔴         ← تفهم 'pending_seller' وتعرضه. تُنشر والخلفية لم تكتبه بعد
                             ⟹ صفر أثر. لكن العكس يعني حالة يعرضها المتصفح «قيد المعالجة»
٤  حراس الخلفية            ← SETTLED_STATUSES · حارس :446 · المميِّز
٥  الإشعارات                ← نقل الكتلة + إشعار البائع
٦  التسوية 🔴               ← **هنا فقط** تبدأ الحالة الجديدة بالظهور. نقطة اللاعودة
٧  n8n                     ← لصق يدوي، خارج نشر Firebase
٨  القواعد والنصوص          ← بعد استقرار السلوك
```

**السبب:** الاستضافة والدوال تُنشران منفصلين. الواجهة أولاً نافذتها **غير ضارّة**؛ الخلفية أولاً نافذتها **تعرض حالة لا يفهمها المتصفح**.

---

# ٦. إجراء التراجع

**التراجع أخطر من التقدّم** — وهذا تعريف النشر غير الآمن. بعد التراجع:

- المُغلِق لا يعيد النظر في القطعة (`:810-812`)
- حارس `settledAt` يجعلها **غير قابلة للتسوية للأبد**
- `authorizeOrderRepair` يرفضها
- وبعد ٢٤ ساعة تُبلَّغ الفائز بكذبة

**⟹ سكربت `repair-pending-seller.cjs` شرط مسبق، لا خطة احتياطية.** خصائصه الإلزامية:

| الخاصية | كيف تُفرَض |
|---|---|
| عديم الأثر بالتكرار | يختار `status=='pending_seller'` فقط؛ بعد الإصلاح لا يُطابَق |
| آمن لإعادة التشغيل | تقريري افتراضياً، `--apply` صراحةً |
| معاملاتي | قراءة وكتابة المزاد والطلب في معاملة واحدة |
| لا طلب مكرّر | `orders/{auctionId}` حتمي + `!orderSnap.exists` |
| لا التزام دفع مكرّر | `paymentDeadlineAt` تُكتب فقط مع إنشاء الطلب |
| لا يحوّل غير مباع إلى بيع | يعيد تشغيل `resolveSettlement`؛ غير `sold` ⟹ حالة نهائية بلا طلب |

نموذجه: `scripts/admin/backfill-order-refs.cjs` + `phase1-census.cjs`.

---

# ٧. مصفوفة الاختبار

| # | السيناريو | يجب أن يَنتج |
|---|---|---|
| A | البائع يقبل (بلغ الحد) | فائز · طلب واحد · مهلة=القبول · كونفيتي |
| B | البائع يرفض | لا فائز · لا طلب · لا دفع · **`auction.status` نهائية** |
| C | البائع لا يفعل شيئاً ٢٤س | كسابقه · **إشعار للطرفين** |
| D | تحت أرضية التسامح | لا بيع · لا عرض · **لا شيء للمزايد** |
| E | المشتري يفتح التطبيق بعد الإغلاق | «أنت أعلى مزايد» · **لا «فزت» ولا «ادفع»** |
| F | القبول قبل الانقضاء بدقيقة | مهلة كاملة من القبول |
| G | عضو على النسخة ١ | **يُطالَب بالنسخة ٢** ← يفشل اليوم |
| H | تعادل عربي/إنجليزي | نفس المعنى التجاري |
| I | قبول + رفض متزامنان | **واحد فقط** |
| J | قبول مكرر | **طلب واحد** |
| K | قبول أثناء كنسة الانقضاء | **لا اكتساح** ← يفشل اليوم |
| L | قبول بعد الانقضاء | فشل ذرّي ✅ يمرّ اليوم |
| M | «الموافقة والنشر» على قطعة بعرض حيّ | **مرفوضة** ← يفشل اليوم |
| N | إصلاح أدمن على قطعة مرفوضة | **مرفوض** ← يفشل اليوم |
| O | ١٠٥ نصوص صحيحة | **لم تُمَس** |
| P | مزاد سابق للنشر `completed` | يبقى فوزاً بلا `belowReserveOffer` |
| Q | `reserve_not_met` تاريخي بمزايد | **لا يصير بيعاً أبداً** |
| R | التراجع + سكربت الإصلاح | كل قطعة عالقة تُحَل |

---

# ٨. الملفات والدوال

**خلفية — حرجة:** `functions/index.js` (`settleAuctionTxn` 363-700 · كتلة الإشعارات 706-757 · `acceptBelowReserve` 3432-3550 · `rejectBelowReserve` 3610-3660 · `expireLapsedOffers` 1258-1315 · `repairEndedAuctionOrder` 3245-3412 · حارس 446) · `settlement.js` · `offerExpiry.js` · `orderRepair.js` · `messageCopy.js` · `emailCopy.js` · `notify.js`

**جديد:** `functions/pendingSellerRepair.js` + `scripts/admin/repair-pending-seller.cjs`

**منطق مشترك:** `bidMath.ts` · `reserveStatus.ts` · `approvalGuard.ts` · `orderStatusGlossary.ts` · `orderWorkflow.ts` · `types.ts`

**خطّافات:** `useMyAuctionLots.ts` · `useSocialProof.ts`

**واجهة:** `SellerCenterView` · `LiveStreamView` · `DesktopLiveAuctionLayout` · `MobileAuctionView` · `DiscoveryFeedView` · `WinCelebration` · `MyOrdersView` · `admin/OurDropsSection` · `DropsListPanel` · `seller/sellerListings.ts`

**نصوص:** `translations.ts` · `auctionRules.ts` (+ آلية قراءة النسخة) · `legalTerms.ts` · `HowItWorksView` · `landing/` · `dropCaption.ts`

**خارج المستودع:** `n8n/build-messages.js` + `webhook-receiver-v2.json`

---

# ما ثبتت سلامته (٦١ بنداً)

حتى لا يضيع الجهد على ما لا يحتاج إصلاحاً: القبول مقابل الرفض المتزامنان **آمنان** · القبول بعد الانقضاء **يفشل ذرّياً** · لا عميل يستطيع تزوير طلب (`firestore.rules:447` `allow create: if false`) · لا مزايدات جديدة تصل قطعة مغلقة · المبيعات التاريخية لا تُعاد قراءتها خطأً · `paymentDeadlineFromNow` مرساتها إنشاء الطلب فلا حساب جديد مطلوب · `auctionTimeStatus` يتدهور بأمان.

---

**الحكم: FAIL. الثماني ضمانات في §٣ إلزامية قبل أي سطر تنفيذ.**
