rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable full client-side operations since session is managed custom/local
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
