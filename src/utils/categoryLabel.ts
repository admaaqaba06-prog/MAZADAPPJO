/**
 * Kept as a module so the existing import sites do not churn. The taxonomy
 * itself moved to `categories.ts` when the seller picker, the drop builder, the
 * concierge form, the Discover chips and the Algolia facet map were unified
 * onto one list.
 */
export { categoryLabel } from './categories';
